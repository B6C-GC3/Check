import "./style.css";
import * as React from 'react';
import { Col } from 'antd';
import { L } from '../../lib/abpUtility';

const Footer = () => {
  return (
    <Col className={"footer"}>
      {//L("FOOTER_TITLE", "COMMON")
      }
    </Col>
  );
};
export default Footer;
